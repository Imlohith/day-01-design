import { makeStyles } from '@material-ui/core';

export const useStyles = makeStyles({
    profile: {
        backgroundColor: '#fff',
        borderRadius: '10px',
        boxShadow: '0 10px 20px rgba(0,0,0,0.19), 0 2px 2px rgba(0,0,0,0.23)',
        padding: 20,
    },
    box: {  
      marginTop: 20,
      backgroundColor: '#fff',
      borderRadius: '10px',
      boxShadow: '0 20px 30px rgba(0,0,0,0.0), 0 8px 8px rgba(0,0,0,0.3)',
      padding: 20,
    }
});